# 自媒体视频自动化工作台 PRD v0.1

> 文档状态：需求澄清版 / 可继续迭代
> 产品方向：本地优先的自媒体视频模板编排工作台
> 当前重点：模板化图文/素材视频、剪映草稿生成、配音字幕对齐、可复用工作流
> 非当前重点：完整实时视频编辑器、全自动 AI 爆款视频生成、复杂口播精剪

---

## 1. 产品一句话定义

一个面向自媒体短视频生产的 **本地优先视频自动化工作台**。

它允许用户上传素材、选择或创建模板、接入已有配音 API、生成字幕、编排时间轴、添加音乐/标题/水印/画中画，并最终输出：

1. 可继续编辑的剪映草稿；
2. 可快速查看的 MP4 预览；
3. 可复用的项目 JSON / 模板 JSON。

本产品不是普通剪辑软件，也不是单纯 AI 视频生成器，而是：

> 剪映草稿生成器 + 视频模板编排器 + 本地素材时间轴工作台 + 可选 AI/Agent 自动化能力。

---

## 2. 产品背景

用户当前有较强的自媒体视频生产需求，视频形态主要包括：

- 一张图贯穿全片的旁白视频；
- 黑底/白底 + 居中图 + 字幕/标题的视频；
- 多张图片轮播或音乐卡点视频；
- 开局一张图，到高潮点再出现另一张图的视频；
- 图片、AI 图、截图、封面图、漫画图等素材型视频；
- 未来可能加入口播自动粗剪、字幕、配音、B-roll 等能力。

当前痛点不是“不会剪视频”，而是：

- 每次都在剪映里重复拖素材、调尺寸、加字幕、对配音、放音乐；
- 多个视频结构高度相似，但无法快速复用；
- 配音、字幕、画面素材、BGM、时间轴之间缺乏自动化联动；
- 视频可以通过剪映精修，但前期粗剪和模板搭建成本高；
- 如果未来做批量内容，需要结构化项目和模板系统支撑。

---

## 3. 产品目标

### 3.1 第一阶段目标

第一阶段要做出一个能真实自用的 MVP：

用户可以创建视频项目，导入素材，选择内置模板或自定义模板，设置画布比例、背景、图片位置、图片时长、字幕、配音、BGM、标题、水印，最终生成剪映草稿，并可选生成 MP4 预览。

### 3.2 中期目标

形成稳定的模板体系和项目数据结构，让用户可以保存、复用、复制、批量替换素材，提升自媒体视频生产效率。

### 3.3 长期目标

成为一个 Agent-ready 的自媒体视频生产工作台。

未来 AI / Agent 不直接“自由剪视频”，而是操作结构化项目模型：

自然语言需求 → 模板选择 → 文案分句 → 配音字幕 → 时间轴生成 → 素材匹配 → 质检修复 → 导出剪映草稿。

---

## 4. 非目标

第一版不做以下内容：

- 不做完整替代剪映的专业剪辑软件；
- 不做实时视频时间线编辑器；
- 不做复杂实时渲染预览；
- 不做完整 AI 一键爆款短视频生成器；
- 不做全平台自动发布；
- 不做复杂多人协同；
- 不做深度口播智能精剪；
- 不强绑定某一个开源项目或技术栈；
- 不把系统核心设计成某个工具的二次封装。

---

## 5. 产品形态判断

### 5.1 推荐产品形态

第一阶段推荐做成：

> 本地优先的桌面应用。

配套形态：

| 形态 | 定位 |
|---|---|
| 桌面应用 | 真正干活的生产工作台 |
| 网站 | 官网、下载页、教程、账号、套餐、模板展示 |
| 网页应用 | 后期轻量模板管理、云端项目查看、协作审核 |

### 5.2 为什么优先桌面应用

本产品高频依赖：

- 本地文件夹上传；
- 批量素材管理；
- 本地音视频文件读写；
- 生成剪映草稿文件夹；
- 调用本地渲染或转码能力；
- 输出项目文件夹；
- 与剪映等本地软件协同。

因此第一版不建议做纯网页 SaaS。网页可以后置，桌面工作台应优先。

### 5.3 技术实现原则

不提前限定死技术栈。

推荐原则：

- UI 可以使用 Web 技术；
- 本地能力可以由桌面壳或本地服务提供；
- 视频生成能力通过适配器接入；
- 剪映草稿只是一个输出目标，不是系统核心；
- 项目 JSON / 模板 JSON 才是产品核心资产。

---

## 6. 核心用户

### 6.1 第一批用户

- 自媒体创作者；
- AI 内容生产者；
- 短视频矩阵号运营者；
- 需要批量做图文视频、旁白视频、知识视频的人；
- 经常使用剪映精修，但希望前期粗剪自动化的人。

### 6.2 典型用户特征

- 有素材和文案，但不想重复剪辑；
- 对视频风格和前端审美有要求；
- 希望保留人工精修空间；
- 需要模板化、批量化、可复用；
- 未来可能接入 AI/Agent 工作流。

---

## 7. 核心使用场景

### 场景 1：单图贯穿旁白视频

用户上传一张图，输入文案，选择配音音色，系统生成配音和字幕。图片从头到尾居中或铺满，字幕在底部，最终导出剪映草稿和 MP4 预览。

### 场景 2：高潮换图视频

用户上传两张图。第一张图从开头显示到后段，第二张图在高潮点出现。高潮点可以手动设置，也可以未来由 AI 根据文案识别。

### 场景 3：多图轮播卡点视频

用户上传一个文件夹，系统按文件名或拖拽顺序排列图片，每张图片显示 1 秒或 2 秒，配 BGM 和字幕，导出草稿。

### 场景 4：黑底白字观点视频

用户选择黑底模板，输入文案，系统生成大字标题、字幕、配音和背景音乐，画面可以只有文字，也可以加一张居中图。

### 场景 5：封面图转动态视频

用户上传一张封面图或 AI 图，系统添加轻微缩放、漂移、字幕、配音和 BGM，让静态图变成可发布视频。

### 场景 6：画中画讲解视频

用户上传背景图/视频和前景素材，系统生成主画面 + 画中画 + 字幕 + 配音 + 水印的视频草稿。

---

## 8. 视频类型抽象

第一版需要支持以下视频结构。

### 8.1 单主图贯穿型

特征：

- 一张图从头到尾存在；
- 可居中、铺满、裁切、留白；
- 可调整大小、位置、比例；
- 可叠加字幕、标题、水印、BGM、配音。

### 8.2 高潮切换型

特征：

- 主图前段存在；
- 高潮点出现第二张图或其他视觉元素；
- 高潮出现方式可选：切换、淡入、放大、闪现、震动；
- 时间点可手动，未来可 AI 推荐。

### 8.3 图片轮播型

特征：

- 多张图片按顺序出现；
- 支持文件夹导入；
- 支持拖拽排序；
- 支持默认时长和单图自定义时长；
- 后续可支持音乐节拍驱动。

### 8.4 背景 + 居中主体型

特征：

- 黑底、白底、纯色、图片、视频、模糊背景；
- 中间放图片、视频、截图、头像、封面；
- 顶部标题、底部字幕、角落水印。

### 8.5 多层画中画型

特征：

- 背景层；
- 主图层；
- 画中画层；
- 字幕层；
- 标题层；
- 水印层；
- 音频层。

第一版可以只做轻量实现，但底层模型必须预留多层能力。

---

## 9. 核心产出物

### 9.1 默认输出结构

每个项目导出后建议生成一个完整项目文件夹：

```text
项目名_日期/
├─ preview.mp4
├─ jianying_draft/
├─ project.json
├─ template.json
├─ subtitles.srt
├─ voiceover.mp3
├─ assets/
└─ export_log.json
```

### 9.2 剪映草稿

核心产物之一。

用途：

- 在剪映中继续精修；
- 调整字幕、动画、转场、音量、特效；
- 保留人工创作空间。

### 9.3 MP4 预览

不是实时预览，而是生成后的低清或标准预览。

用途：

- 快速检查比例；
- 检查字幕；
- 检查配音；
- 检查图片位置；
- 检查高潮图出现时间；
- 检查整体节奏。

### 9.4 项目 JSON

工作台自己的源文件。

用途：

- 重新打开项目；
- 重新导出；
- 替换素材；
- 保存模板；
- 给未来 AI/Agent 修改。

### 9.5 模板 JSON

保存可复用视频结构。

用途：

- 快速创建同类视频；
- 复用布局、字幕、时间规则、配音配置；
- 支撑后续模板库和模板市场。

---

## 10. 预览策略

### 10.1 第一版不做实时视频编辑预览

不做：

- 实时时间轴播放；
- 拖动实时渲染；
- 类似剪映的完整预览窗口；
- 实时音频波形卡点；
- 实时多轨编辑器。

### 10.2 第一版建议做低成本预览

优先级：

1. 静态布局预览；
2. 生成后低清 MP4 预览；
3. 后续再考虑局部实时预览。

### 10.3 静态布局预览

用于查看：

- 画布比例；
- 主图位置；
- 图片裁切；
- 标题位置；
- 字幕位置；
- 水印位置。

### 10.4 低清 MP4 预览

用于查看：

- 配音字幕是否大体对齐；
- BGM 音量是否合适；
- 图片切换是否正确；
- 高潮图出现时间是否正确；
- 整体节奏是否成立。

---

## 11. 模板系统设计

模板系统是核心资产，不是附属功能。

### 11.1 模板类型

| 模板类型 | 说明 |
|---|---|
| 画布模板 | 保存比例、分辨率、背景、安全区 |
| 视觉布局模板 | 保存元素位置、层级、大小、对齐方式 |
| 时间轴模板 | 保存素材出现规则和持续时间 |
| 字幕模板 | 保存字幕样式、位置、描边、字号 |
| 配音模板 | 保存配音 API 参数、音色、语速、情绪 |
| 工作流模板 | 保存完整生产流程 |

### 11.2 用户保存模板时的选项

用户可以选择保存：

1. 只保存样式；
2. 保存结构；
3. 保存完整项目；
4. 保存工作流。

### 11.3 内置模板建议

第一版至少内置以下模板：

#### 模板 1：单图贯穿旁白模板

- 一张图从头到尾；
- 可居中或铺满；
- 底部字幕；
- 可配音；
- 可加 BGM；
- 可加水印。

#### 模板 2：高潮换图模板

- 第一张图前段显示；
- 第二张图在指定时间出现；
- 支持淡入、切换、放大；
- 可设置高潮出现时间。

#### 模板 3：图片轮播模板

- 多图按顺序出现；
- 默认每张图 1 秒或 2 秒；
- 支持拖拽排序；
- 支持转场；
- 支持背景音乐。

#### 模板 4：黑底白字旁白模板

- 黑底或白底；
- 大字标题；
- 旁白字幕；
- 配音；
- BGM。

#### 模板 5：封面图转视频模板

- 一张封面图；
- 轻微缩放或漂移；
- 字幕层；
- 配音层；
- 最后定格。

#### 模板 6：画中画讲解模板

- 背景；
- 主素材；
- 画中画；
- 字幕；
- 标题；
- 水印。

---

## 12. 功能优先级

### 12.1 P0：MVP 必须做

#### 项目创建

- 新建项目；
- 设置项目名称；
- 设置画布比例；
- 设置分辨率；
- 设置帧率；
- 设置导出目录。

#### 素材导入

- 支持上传单个文件；
- 支持上传多个文件；
- 支持上传文件夹；
- 支持图片、视频、音频、字幕、水印、字体；
- 素材进入素材库。

#### 基础画布

- 支持 9:16；
- 支持 16:9；
- 支持 1:1；
- 支持 4:5；
- 支持黑底、白底、纯色背景；
- 支持背景图或背景视频，若开发成本可控。

#### 图片编排

- 单图贯穿全程；
- 多图轮播；
- 图片拖拽排序；
- 每张图片设置时长；
- 图片位置调整；
- 图片缩放；
- 图片适配方式：铺满、居中、等比缩放、裁切、留白。

#### 字幕

- 支持手动输入文案；
- 支持导入 SRT；
- 支持根据文案生成字幕结构；
- 支持字幕位置；
- 支持基础字幕样式；
- 支持字幕导出为 SRT。

#### 配音

- 接入已有配音 API；
- 支持选择音色；
- 支持设置语速；
- 支持设置音量；
- 支持根据分句生成配音；
- 支持保存配音文件。

#### 音乐

- 支持添加 BGM；
- 支持设置音量；
- 支持裁剪或循环；
- 支持淡入淡出，若成本可控。

#### 标题与水印

- 支持顶部标题；
- 支持固定水印；
- 支持位置调整；
- 支持大小调整；
- 支持开关控制。

#### 模板

- 支持选择内置模板；
- 支持保存当前项目为模板；
- 支持从模板新建项目；
- 支持模板参数面板。

#### 导出

- 支持导出剪映草稿；
- 支持导出 project.json；
- 支持导出 template.json；
- 支持导出字幕文件；
- 建议支持导出低清 MP4 预览；
- 支持打开输出文件夹。

---

### 12.2 P1：第一版后续增强

- 高潮换图模板；
- 高潮出现时间设置；
- 画中画图层；
- 字幕样式模板；
- 配音模板；
- 批量替换素材；
- 复制项目；
- 模板库管理；
- 静态布局预览；
- 低清 MP4 预览优化；
- 项目历史记录；
- BGM 节拍辅助标记；
- 批量生成多个版本。

---

### 12.3 P2：中长期功能

- 音乐自动卡点；
- AI 自动识别高潮句；
- AI 自动推荐模板；
- AI 自动素材排序；
- AI 自动质检；
- 口播自动粗剪；
- 静音剪辑；
- 字幕口播识别；
- 自动插入 B-roll；
- Agent 批量生成视频；
- MCP Server；
- 模板市场；
- 云端模板同步；
- 团队协作审核。

---

## 13. 页面与信息架构

### 13.1 首页 / 项目中心

功能：

- 新建项目；
- 打开项目；
- 最近项目；
- 模板入口；
- 输出目录入口；
- 设置入口。

### 13.2 新建项目页

用户选择：

- 项目名称；
- 画布比例；
- 分辨率；
- 初始模板；
- 输出位置。

### 13.3 项目编辑页

主要区域：

1. 左侧素材库；
2. 中间画布/静态预览；
3. 右侧参数面板；
4. 底部简化时间轴；
5. 顶部导出/保存/预览按钮。

### 13.4 模板库页

功能：

- 查看内置模板；
- 查看用户模板；
- 从模板创建项目；
- 编辑模板；
- 删除模板；
- 导入/导出模板 JSON。

### 13.5 配音与字幕页/面板

功能：

- 输入文案；
- 分句；
- 选择音色；
- 生成配音；
- 生成字幕；
- 调整字幕样式；
- 导出 SRT。

### 13.6 导出页/面板

功能：

- 选择导出目标；
- 剪映草稿；
- MP4 预览；
- 项目 JSON；
- 模板 JSON；
- 输出目录；
- 导出日志；
- 打开输出文件夹。

### 13.7 设置页

功能：

- 配音 API 配置；
- 默认输出目录；
- 默认画布；
- 默认字幕样式；
- 默认 BGM 音量；
- 本地工具路径；
- 缓存管理。

---

## 14. 核心数据模型

### 14.1 Project

```json
{
  "id": "project_001",
  "name": "高考志愿单图旁白视频",
  "version": "0.1",
  "canvas": {
    "ratio": "9:16",
    "width": 1080,
    "height": 1920,
    "fps": 30,
    "background": {
      "type": "color",
      "value": "#000000"
    }
  },
  "duration": 28.5,
  "templateId": "template_single_image_voiceover",
  "assets": [],
  "layers": [],
  "segments": [],
  "audio": {},
  "subtitles": [],
  "exportTargets": ["jianying_draft", "mp4_preview"]
}
```

### 14.2 Asset

```json
{
  "id": "asset_001",
  "type": "image",
  "name": "cover.png",
  "path": "assets/cover.png",
  "metadata": {
    "width": 1024,
    "height": 1536,
    "duration": null
  }
}
```

### 14.3 Layer

```json
{
  "id": "layer_main_image",
  "type": "visual",
  "name": "主图层",
  "zIndex": 10,
  "visible": true,
  "locked": false
}
```

### 14.4 Segment

```json
{
  "id": "segment_001",
  "assetId": "asset_001",
  "layerId": "layer_main_image",
  "start": 0,
  "end": 28.5,
  "transform": {
    "x": 540,
    "y": 960,
    "scale": 0.85,
    "rotation": 0,
    "fit": "contain"
  },
  "animation": {
    "in": null,
    "out": null,
    "keyframes": []
  }
}
```

### 14.5 Subtitle

```json
{
  "id": "subtitle_001",
  "text": "很多家长第一步就错了",
  "start": 0.2,
  "end": 2.1,
  "styleId": "subtitle_style_default"
}
```

### 14.6 Template

```json
{
  "id": "template_single_image_voiceover",
  "name": "单图贯穿旁白模板",
  "type": "workflow",
  "canvas": {
    "ratio": "9:16",
    "background": "black"
  },
  "layers": [
    {
      "type": "main_image",
      "durationRule": "full_video",
      "position": "center",
      "scale": 0.85,
      "fit": "contain"
    },
    {
      "type": "subtitle",
      "position": "bottom"
    },
    {
      "type": "watermark",
      "position": "top_right"
    }
  ],
  "audio": {
    "voiceover": true,
    "bgm": true
  },
  "export": {
    "jianyingDraft": true,
    "mp4Preview": true
  }
}
```

---

## 15. 工作流设计

### 15.1 单图贯穿工作流

1. 新建项目；
2. 选择单图贯穿模板；
3. 上传主图；
4. 输入文案；
5. 调用配音 API；
6. 生成字幕；
7. 设置图片大小和位置；
8. 添加 BGM；
9. 生成静态预览；
10. 导出 MP4 预览；
11. 导出剪映草稿。

### 15.2 高潮换图工作流

1. 新建项目；
2. 选择高潮换图模板；
3. 上传主图和高潮图；
4. 输入文案；
5. 设置高潮出现时间；
6. 设置高潮图动画；
7. 添加配音字幕；
8. 添加 BGM；
9. 导出预览；
10. 导出剪映草稿。

### 15.3 多图轮播工作流

1. 新建项目；
2. 选择图片轮播模板；
3. 上传文件夹；
4. 自动生成素材列表；
5. 用户拖拽排序；
6. 设置每张图默认时长；
7. 单独调整部分图片时长；
8. 添加 BGM；
9. 添加标题/水印；
10. 导出草稿。

---

## 16. AI / Agent 功能规划

### 16.1 原则

第一版不把 AI 作为主功能，但底层结构必须 Agent-ready。

AI / Agent 不直接写剪映草稿，不直接自由调用底层命令，而是通过受控工具修改 project.json。

### 16.2 未来 AI 能力

| 阶段 | 能力 |
|---|---|
| V1 | 文案分句、字幕优化、标题生成 |
| V1 | 模板推荐、参数填充 |
| V1.5 | 高潮句识别、高潮点推荐 |
| V2 | 素材排序、素材匹配 |
| V2 | 质检报告和自动修复建议 |
| V3 | 批量生成多个视频版本 |
| V4 | MCP Server，供 Codex / Claude Code 等 Agent 调用 |

### 16.3 Agent 工具接口设想

未来可以提供受控工具：

```text
create_project()
select_template()
import_assets()
set_canvas()
set_layer_position()
set_segment_duration()
generate_voiceover()
generate_subtitles()
choose_climax_point()
render_preview()
export_jianying_draft()
run_quality_check()
```

Agent 只能通过这些工具操作项目，不能任意写文件或执行系统命令。

---

## 17. 开放式技术路线建议

### 17.1 技术原则

- 不强绑定某一个开源项目；
- 不把剪映草稿结构当成唯一核心；
- 先定义项目模型；
- 再做输出适配器；
- 配音 API 做成可替换接口；
- 渲染、草稿生成、字幕生成都做模块化。

### 17.2 推荐架构

```text
用户界面
  ↓
项目编辑器
  ↓
统一视频项目模型
  ↓
模板系统
  ↓
时间轴编排系统
  ↓
配音 / 字幕 / 音乐 / 素材处理
  ↓
输出适配器
  ├─ 剪映草稿
  ├─ MP4 预览
  ├─ CapCut 草稿
  ├─ Remotion 渲染
  ├─ FFmpeg 渲染
  └─ 未来其他格式
```

### 17.3 可选技术方向

以下仅为方向，不是强制选型：

| 模块 | 可选方向 |
|---|---|
| 桌面壳 | Electron / Tauri / 本地 Web UI |
| 前端 UI | React / Vue / Svelte |
| 本地服务 | Node.js / Python / Rust / Go |
| MP4 合成 | FFmpeg / Remotion / MoviePy |
| 剪映草稿 | pyJianYingDraft / 自研适配器 / 其他 CapCut 草稿工具 |
| 字幕识别 | faster-whisper / WhisperX / 第三方 API |
| 配音 | 已有配音 API |
| Agent 接入 | OpenAI API / Claude / 本地 MCP Server |

---

## 18. 安全与稳定性要求

### 18.1 文件安全

- 不覆盖用户原始素材；
- 默认复制素材到项目目录或建立受控引用；
- 导出目录可配置；
- 每次导出生成日志；
- 出错时保留错误信息。

### 18.2 API Key 安全

- 配音 API Key 不明文展示；
- 本地加密保存；
- 支持测试连接；
- 支持清除配置。

### 18.3 Agent 安全

未来接 Agent 时：

- Agent 不能任意执行 shell；
- Agent 不能删除用户文件；
- Agent 只能写入项目目录；
- 所有 project.json 必须 schema 校验；
- 导出操作必须有日志；
- 关键批量操作需要用户确认。

### 18.4 草稿兼容性

- 剪映草稿生成可能受版本影响；
- 需要记录支持的剪映版本；
- 导出失败时给出可理解错误；
- 保留 project.json 以便后续重新生成。

---

## 19. MVP 验收标准

### 19.1 基础验收

MVP 至少能完成以下任务：

1. 新建 9:16 项目；
2. 上传一张图片；
3. 输入文案；
4. 调用配音 API 生成配音；
5. 生成字幕；
6. 图片从头到尾显示；
7. 字幕显示在底部；
8. 添加 BGM；
9. 导出剪映草稿；
10. 导出 project.json；
11. 保存当前配置为模板；
12. 用模板创建新项目。

### 19.2 增强验收

如果资源允许，MVP 还应支持：

1. 生成低清 MP4 预览；
2. 上传多张图片并排序；
3. 设置每张图片时长；
4. 高潮点出现第二张图；
5. 标题和水印层；
6. 静态布局预览。

---

## 20. 版本路线图

### V0：技术验证版

目标：

- 跑通 project.json → 剪映草稿；
- 跑通单图贯穿；
- 跑通配音 API；
- 跑通字幕文件；
- 跑通导出目录。

不要求漂亮 UI。

### V0.5：自用 MVP

目标：

- 有基础桌面界面；
- 能选择模板；
- 能上传素材；
- 能调图片位置和大小；
- 能生成配音字幕；
- 能导出草稿；
- 能保存模板。

### V1：可用产品版

目标：

- 模板库；
- 多图轮播；
- 高潮换图；
- 静态预览；
- 低清 MP4 预览；
- 项目管理；
- 基础设置页；
- 错误日志。

### V1.5：AI 辅助版

目标：

- 文案分句；
- 字幕优化；
- 模板推荐；
- 高潮句识别；
- 批量生成变体。

### V2：Agent 工作台版

目标：

- 半自动项目创建；
- 自动素材排序；
- 质检修复；
- 批量视频生成；
- 受控 Agent 工具调用。

### V3：开放生态版

目标：

- 本地 MCP Server；
- 模板市场；
- 云端模板同步；
- 外部 Agent 调用；
- 多平台输出适配器。

---

## 21. 第一版最小开发任务拆分

### 21.1 项目模型

- 定义 project.json schema；
- 定义 template.json schema；
- 定义 asset/layer/segment/subtitle/audio 数据结构；
- 定义导出日志结构。

### 21.2 素材系统

- 文件导入；
- 文件夹导入；
- 素材复制或引用；
- 素材元信息读取；
- 素材列表管理。

### 21.3 模板系统

- 内置模板读取；
- 用户模板保存；
- 从模板创建项目；
- 模板参数编辑。

### 21.4 配音字幕

- 接入已有配音 API；
- 文案分句；
- 生成配音；
- 生成 SRT；
- 字幕样式保存。

### 21.5 画布与布局

- 画布比例；
- 背景；
- 图片 fit 模式；
- 图片位置；
- 图片缩放；
- 标题/水印位置。

### 21.6 导出系统

- project.json 保存；
- template.json 保存；
- 剪映草稿适配器；
- MP4 预览适配器；
- 导出日志；
- 打开输出文件夹。

---

## 22. 关键产品判断

1. 第一版的核心不是 AI，而是结构化视频项目模型。
2. 模板系统是产品资产，应从第一版开始设计。
3. 剪映草稿是最重要的输出物，但不应成为内部唯一模型。
4. MP4 预览不做实时编辑器，只做生成后验收。
5. 桌面应用比纯网页更符合当前需求。
6. AI/Agent 后续应操作 project.json，而不是直接操作底层剪辑工具。
7. 本产品应保持技术路线开放，不把实现路径写死。

---

## 23. 当前待确认问题

1. 第一版是否必须输出 MP4 预览，还是 V0 只输出剪映草稿？
2. 第一版内置模板数量是否控制在 3 个以内？
3. 配音 API 的输入输出格式是什么？
4. 剪映草稿导出优先支持哪个系统：Windows 还是 macOS？
5. 用户素材是复制进项目目录，还是保留本地路径引用？
6. 是否需要账号系统，还是第一版完全本地单机？
7. 是否需要模板云同步，还是先只做本地模板？
8. 第一版是否需要 AI 文案分句，还是只做基础标点分句？
9. 输出草稿是否需要兼容特定剪映版本？
10. 是否需要支持批量生成多条视频，还是先单项目单视频？

---

## 24. 推荐当前确认版产品定义

> 第一阶段产品定位：一个本地优先的自媒体视频自动化工作台。它以模板和时间轴为核心，支持用户上传素材、接入已有配音 API、配置字幕和音乐、调整画面元素位置与时长，并输出剪映草稿或 MP4 预览。第一版重点不是 AI 自动创作，而是把高频视频结构模板化、可视化、可复用。

---

## 25. 推荐 MVP 范围

### 必做

- 桌面工作台雏形；
- 项目 JSON；
- 模板 JSON；
- 单图贯穿模板；
- 多图轮播模板；
- 基础字幕；
- 接入配音 API；
- BGM；
- 标题/水印；
- 剪映草稿导出；
- 本地模板保存。

### 建议做

- 高潮换图模板；
- 低清 MP4 预览；
- 静态布局预览；
- 项目导出文件夹；
- 导出日志。

### 暂缓

- 实时视频编辑器；
- 复杂音乐卡点；
- 口播自动精剪；
- Agent 自动生成；
- 云端协作；
- 模板市场。
